{
  "setup-worktree": [
    "git submodule update --init --recursive",
    "cp \"$ROOT_WORKTREE_PATH/.env\" .env",
    "ln -sf \"$ROOT_WORKTREE_PATH/.venv\" .venv",
    "mkdir -p logs/orchestra && ln -sf \"$ROOT_WORKTREE_PATH/logs/orchestra\" logs/orchestra/_root"
  ]
}
